# Ignore exports folder contents but ensure it exists
exports/*
!exports/.DO_NOT_IGNORE

# Autogenerated data
.import/
*.translation

# Ignore this file to avoid keystore passwords being exposed
# If you wanna commit your configuration but have sensitive data
# copy the contents to export_presets_template.cfg 
# and do the following regex replace 
#
# find every line containing "keystore" or "password" and a non-empty value
# (.*((keystore)|(password)).*)="(.+)"
#
# and replace the value with an empty string:
# $1=""
export_presets.cfg

# Android build ignore
android/build/*
!android/build/AndroidManifest.xml
